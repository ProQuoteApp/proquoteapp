/// Constants used throughout the app
class AppConstants {
  /// Standard screen padding for consistent margins
  static const double screenPadding = 16.0;
  
  /// Standard card border radius
  static const double cardBorderRadius = 12.0;
  
  /// Standard spacing between sections
  static const double sectionSpacing = 24.0;
  
  /// Standard spacing between items
  static const double itemSpacing = 16.0;
  
  /// Standard spacing between text elements
  static const double textSpacing = 8.0;
  
  /// Standard icon size
  static const double iconSize = 24.0;
  
  /// Standard small icon size
  static const double smallIconSize = 16.0;
  
  /// Standard avatar size (radius)
  static const double avatarRadius = 60.0;
  
  /// Standard small avatar size (radius)
  static const double smallAvatarRadius = 24.0;
  
  /// Private constructor to prevent instantiation
  AppConstants._();
} 